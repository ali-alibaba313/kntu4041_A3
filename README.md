# 🗺️ WebGIS Final Project

پروژه نهایی درس سامانه‌های اطلاعات جغرافیایی وب‌محور (WebGIS)

## 📋 توضیحات پروژه

این پروژه یک سامانه WebGIS کامل با استفاده از Flask و OpenLayers است که قابلیت‌های زیر را دارد:

- ✅ احراز هویت کاربران (ورود/ثبت‌نام)
- ✅ نمایش لایه‌های WMS از GeoServer
- ✅ قابلیت GetFeatureInfo با کلیک روی نقشه
- ✅ رابط کاربری مدرن و ریسپانسیو

## 🛠️ فناوری‌های استفاده شده

- **Backend:** Flask (Python)
- **Frontend:** HTML, CSS, JavaScript
- **Map Library:** OpenLayers 8.2
- **GIS Server:** GeoServer (WMS)

## 📁 ساختار پروژه
Final_Project/

├── app.py # فایل اصلی Flask با routing و authentication

├── requirements.txt # وابستگی‌های Python

├── README.md # مستندات پروژه (همین فایل)

├── static/

│ ├── css/

│ │ └── style.css # استایل‌های CSS (مدرن و ریسپانسیو)

│ └── js/

│ └── map.js # کد JavaScript نقشه (OpenLayers)

└── templates/

├── login.html # صفحه ورود کاربر

├── register.html # صفحه ثبت‌نام کاربر

└── map.html # صفحه نمایش نقشه

🚀 نصب و راه‌اندازی
پیش‌نیازها
Python 3.8 یا بالاتر
GeoServer (در حال اجرا روی localhost:8080)
مرورگر مدرن (Chrome, Firefox, Edge)
مراحل نصب
1. کلون کردن پروژه:

bash

git clone <repository-url>

cd Final_Project

2. ایجاد محیط مجازی (اختیاری ولی توصیه می‌شود):

bash

python -m venv venv

source venv/bin/activate # در Linux/Mac

venv\Scripts\activate # در Windows

3. نصب وابستگی‌ها:

bash

pip install -r requirements.txt

4. اجرای برنامه:

bash

python app.py

5. باز کردن مرورگر:

http://127.0.0.1:5000

⚙️ پیکربندی GeoServer
برای اتصال به GeoServer خودتان، فایل static/js/map.js را باز کنید و تنظیمات CONFIG را تغییر دهید:

javascript

const CONFIG = {

geoserver: {

url: ‘http://localhost:8080/geoserver/wms’,

workspace: ‘YOUR_WORKSPACE’,

layerName: ‘YOUR_LAYER’,

fullLayerName: ‘workspace:layer’

},

map: {

center: [51.4, 35.7], # مختصات مرکز تهران

zoom: 12,

minZoom: 5,

maxZoom: 20

}

};

راهنمای پیکربندی:
url: آدرس سرویس WMS در GeoServer
workspace: نام workspace در GeoServer
layerName: نام لایه
fullLayerName: ترکیب workspace و layerName (workspace:layer)
📝 نحوه استفاده
1. ثبت‌نام:

روی دکمه “ثبت‌نام” کلیک کنید
نام کاربری، ایمیل و رمز عبور وارد کنید
رمز عبور باید حداقل 6 کاراکتر باشد
2. ورود:

نام کاربری و رمز عبور خود را وارد کنید
روی “ورود” کلیک کنید
3. استفاده از نقشه:

روی نقشه کلیک کنید تا اطلاعات عارضه نمایش داده شود
از دکمه‌های زوم و ابزارهای نقشه استفاده کنید
اطلاعات کاربر در هدر نمایش داده می‌شود
4. خروج:

روی دکمه “خروج” در هدر کلیک کنید
🔐 امنیت
این پروژه از روش‌های امنیتی زیر استفاده می‌کند:

هش کردن رمز عبور: با الگوریتم SHA-256
Session Management: استفاده از Flask Session
Secret Key: تولید امن با secrets.token_hex(32)
Validation: اعتبارسنجی ورودی‌ها در سمت سرور و کلاینت
⚠️ نکته مهم: این پروژه برای اهداف آموزشی است. برای استفاده تولیدی، موارد زیر را اضافه کنید:

استفاده از HTTPS
هش قوی‌تر (bcrypt به جای SHA-256)
پایگاه داده واقعی (PostgreSQL به جای حافظه موقت)
محدودیت تعداد تلاش‌های ورود
CSRF Protection
🎨 ویژگی‌های رابط کاربری
طراحی مدرن: با استفاده از CSS Variables و Animations
ریسپانسیو: سازگار با موبایل، تبلت و دسکتاپ
دارک مود: رنگ‌بندی حرفه‌ای با گرادیانت
اعتبارسنجی زنده: فیدبک فوری به کاربر
Loading States: نمایش وضعیت در حال بارگذاری
🐛 عیب‌یابی
مشکل: GeoServer در دسترس نیست
راه‌حل:

مطمئن شوید GeoServer در حال اجرا است
آدرس URL در فایل map.js را بررسی کنید
Console مرورگر را برای خطاهای CORS چک کنید
مشکل: نقشه نمایش داده نمی‌شود
راه‌حل:

مختصات مرکز و زوم را بررسی کنید
نام workspace و layer را تایید کنید
Network Tab در Developer Tools را چک کنید
مشکل: ورود کار نمی‌کند
راه‌حل:

Browser cookies را فعال کنید
Cache مرورگر را پاک کنید
مطمئن شوید Flask در حال اجرا است
📚 منابع مفید
Flask Documentation
OpenLayers Documentation
GeoServer Documentation
👨‍💻توسعه دهندگان 
[علی علی بابا - علیرضا سرو] 
📄 لایسنس
این پروژه برای اهداف آموزشی توسعه داده شده است و تحت لایسنس MIT منتشر شده است.

🙏 تشکر